menu "Peripheral Drivers"
    config DRIVER_SYS_UART
        bool "SYS UART Driver Support"
        # [Required] TODO_BSP_REQUIRED: remove this marker after sys_uart.c is implemented; use n otherwise.
        default y

    config DRIVER_GPIO
        bool "GPIO Driver Support"
        # [Required] TODO_BSP_REQUIRED: remove this marker after gpio.c is implemented; use n otherwise.
        default y

    # [Optional] Add switches here as other HAL implementations become available.
endmenu
