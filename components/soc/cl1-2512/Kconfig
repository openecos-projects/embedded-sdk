config SOC_CL1_2512
    bool
    default y

menu "Peripheral Drivers"
    config DRIVER_SYS_UART
        bool "SYS_UART Driver Support"
        default y

    config DRIVER_GPIO
        bool "GPIO Driver Support"
        default y

    config DRIVER_I2C
        bool "I2C Driver Support"
        default y

    config DRIVER_TIMER
        bool "Timer Driver Support"
        default y
endmenu
